class BootScene extends Phaser.Scene {

    constructor() {
        super("BootScene");
    }

    create() {
        console.log("BOOTSCENE FUNCIONA");

        this.add.text(100, 100, "LOS 7 CUARZOS", {
            fontSize: "50px",
            fill: "#00ffff"
        });
    }
}

window.BootScene = BootScene;
